# Project State — PratiBimb

> **MANDATORY first read for every session, after `AGENTS.md`.**
> Update at the end of every session.

---

## Snapshot

| Field | Value |
|---|---|
| **Date** | 2026-09-07 |
| **Phase** | Phase 1 — Week-1 capability spikes (in progress) |
| **Sprint** | Sprint 1 — Week-1 capability spikes. **S-01 merged (ACCEPT). S-01b merged (CONDITIONAL). B-02 answered (CONDITIONAL, PR #9 open). S-02 BLOCKED — no Firefox.** |
| **Health** | 🟠 **AMBER** — S-01 resolved, but the gating spike S-02 cannot run and Invariant E's test vehicle is measurably blind to the send path. Neither is a code defect; both stop defined work. |
| **Next milestone** | **`v0.2.0-spikes`** — blocked on S-02, which cannot run without Firefox (**B-01**). Then S-03 … S-07, then the 20-cell matrix. |
| **Product code written** | **None. Zero.** |

---

## Evidence recorded so far

| Artifact | What it establishes |
|---|---|
| `artifacts/experiments/ENV-0001-workspace-environment.md` | The workspace was empty; the toolchain present; the GPU is 8 GB |
| `artifacts/reviews/AUDIT-0001-agentos-framework.md` | What Raptor's Way actually implements, and what PratiBimb adopts from it |
| `artifacts/experiments/W1-S01-chrome-webgpu-context/` | **S-01 ACCEPT** — WebGPU is fully functional in a dedicated worker inside a Chrome MV3 offscreen document, across 3 runs. Chrome selects integrated graphics. Chrome 152 refuses `--load-extension`. **Workstation 1 only.** |
| `artifacts/environment/ENV-0002-workstation-omen-audit.md` | **Workstation 2** — AMD integrated graphics, no Docker, no WSL, no Firefox. Establishes that S-01's adapter finding cannot transfer. |
| `artifacts/experiments/W1-S01b-playwright-extension-loading/` | **S-01b CONDITIONAL** — Playwright's `context.route()` does not observe or block egress from the MV3 offscreen document. 3 runs of 3. **Workstation 2 only.** Merged `d7fe11d`. |
| `artifacts/experiments/W1-B02-invariant-e-observation/` *(open PR #9)* | **B-02 CONDITIONAL** — CDP attached to the offscreen target observes and genuinely blocks; an independent loopback collector proves arrival, detects an unauthorised sender by absent provenance, and caught a tampered payload by recomputing its hash. **Workstation 2 only.** |
| `artifacts/experiments/W1-S02-firefox-webgpu-context/` | **No result.** A pre-registered protocol and accept/reject criteria for S-02, fixed before any data exists. **Nothing in it may be cited as a result.** |

## Repository

| Field | Value |
|---|---|
| Remote | `https://github.com/ronitsaha11/pratibimb` — **PUBLIC** |
| Default branch | `main` — **NOT protected.** No rule requires a PR, blocks force-push or deletion, or makes any check required. → **B-05**, issue #6 |
| Checkpoint | `v0.1.0-foundation` = `7a31857` |
| Workflow | `CONTRIBUTING.md` · `docs/operations/git-workflow.md` · **blockers: `agentos/blockers.md`** |
| Contribution topology | Direct push (`ronitsaha11`) **and** fork → PR (contributors with read access). See `docs/operations/git-workflow.md` §8. |

> **Both fields above were previously recorded incorrectly** — the repository was
> described as private and `main` as protected. Neither was true, and both were verified
> against the API rather than assumed:
> `gh repo view --json isPrivate` → `false` ·
> `gh api repos/.../branches/main/protection` → **404 Not Found**.
> The originals are preserved in the history of this file (`git log -p agentos/state.md`).
> **This matters beyond tidiness: this is a privacy product, the repository is
> world-readable, and a claimed branch protection that does not exist is worse than a
> known-absent one, because reviewers stop checking for it.**

## What exists

- The PratiBimb AgentOS engineering layer (this repository).
- The governing dossier, recorded at `docs/dossier/`.
- Frozen contracts, the architecture constitution, specialist contracts, workflows, gates.
- The model registry and the feasibility matrix — **structures only, all cells `UNKNOWN`**.

## What does not exist

- Any extension code. Any server code. Any model asset. Any test.
- Any product measurement — no model has been run, no latency measured.
- Any verified licence.
- Any evidence that WebGPU, ONNX Runtime Web, or any of the five models function in the
  target contexts.

---

## Active work

| Task | Status | Owner | Blocked by |
|---|---|---|---|
| S-01 — WebGPU in a Chrome MV3 offscreen document | `DONE` — ACCEPT, merged in PR #1 (`2fb4e82`) | `browser-engineer` | — |
| S-01b — Playwright / Invariant E interception coverage | `DONE` — CONDITIONAL, merged `d7fe11d` | `browser-engineer` · `evaluation-qa-engineer` | — |
| B-02 — which mechanism can be trusted to enforce Invariant E | `IN_REVIEW` — CONDITIONAL, evidence in **open PR #9** | `browser-engineer` · `evaluation-qa-engineer` | Maintainer merge (**B-04**) |
| S-02 — WebGPU in a Firefox MV3 event page | `BLOCKED` | `browser-engineer` | **B-01** — no Firefox on any workstation |
| Invariant E enforcement vehicle | `BLOCKED` | `privacy-security-engineer` → human architect | **B-02** — needs an ADR, issue #5 |
| Server host decision, then E-01 / E-02 | `BLOCKED` | Human — team | **B-03** |

---

## Blockers

> **`agentos/blockers.md` is the register of record.** The table below is the original
> Phase-0 list, kept for continuity and mapped onto the register rather than deleted.
> When the two disagree, **the register wins**.
>
> | Here | Register | |
> |---|---|---|
> | 1 | *(closed)* | B-00 — workspace was not a git repository |
> | 2, 3, 4 | **B-03** | No vLLM host; 8 GB VRAM; Linux-only platform |
> | 5 | **B-01** | Firefox / Firefox-on-Linux |
> | — | **B-02** | Invariant E enforcement vehicle *(new, from S-01b)* |
> | — | **B-04** | Fork PRs can neither merge nor run CI *(new)* |
> | — | **B-05** | `main` unprotected *(new)* |
> | — | **B-06** | Line endings unmanaged *(new)* |

| # | Blocker | Severity | Resolution path |
|---|---|---|---|
| ~~1~~ | ~~Workspace is not a git repository.~~ **RESOLVED** — initialized and pushed to a **public** GitHub remote, `v0.1.0-foundation` tagged. (**Correction:** this line previously also said the remote was private and `main` protected. Neither was true; `main` is still unprotected — **B-05**.) | ✅ | — |
| 2 | **8 GB VRAM (RTX 5050 Laptop) is tight for Qwen3-VL-4B under vLLM.** FP16 weights alone are ~8 GB before KV cache. Needs a quantised serving path, a reduced context budget, or another host. | 🟡 Major | Spike E-02 · see `artifacts/experiments/ENV-0001` |
| 3 | **vLLM's supported platform is Linux.** Whether it runs here — native, WSL2, or Docker with GPU passthrough — is UNKNOWN. | 🟡 Major | Spike E-01 |
| 4 | No separate GPU host identified for the team | 🟡 Major | Human decision — spike E-04 |
| 5 | **Firefox-on-Linux test environment not identified**, and it is the most likely judging configuration | 🟡 Major | Human decision — spike E-05 |

### Machines — two, and no result transfers between them

| | **Workstation 1** (`ENV-0001`) | **Workstation 2** (`ENV-0002`) |
|---|---|---|
| CPU / integrated GPU | Intel · Intel Graphics | **AMD Ryzen AI 7 350 · Radeon 860M** |
| Discrete GPU | RTX 5050 Laptop, 8151 MiB | RTX 5050 Laptop, 8151 MiB |
| Node · Python | 24.19.0 · 3.13.14 | **26.4.0 · 3.12.10** |
| Docker / WSL | Docker 29.6.2 | **neither** |
| Browsers | not recorded | Chrome 152, Edge 152, **no Firefox** |
| Working tree | OneDrive-synced path | plain local path |
| Evidence recorded on it | S-01 | S-01b, ENV-0002 |

**Client-side work is unblocked on both. Server-side work is blocked on both** (B-03).

Per `AGENTS.md` §5 these are **separate experimental cells**. S-01 recorded Chrome
selecting `intel / gen-12lp`; workstation 2 has no Intel GPU, so that finding cannot
reproduce there and is not a fact about it. Neither machine's numbers generalise to the
other.

---

## Open UNKNOWNs gating work

All of `docs/architecture/constitution.md` section 9, and **all twenty model cells** of
`agentos/registry/feasibility-matrix.md`, remain `UNKNOWN`. **No model has been downloaded
or run.** The only promotions to `FACT` so far are prerequisite spikes, not model cells.

The two that gated everything else:

- ~~**S-01** — WebGPU adapter inside a Chrome `chrome.offscreen` document.~~
  **RESOLVED: FACT, ACCEPT.** See `artifacts/experiments/W1-S01-chrome-webgpu-context/`.
- **S-02** — WebGPU adapter inside a Firefox MV3 event page. **Still `UNKNOWN`. Next.**

New UNKNOWNs raised by S-01:

- **S-01a** — can Chrome be made to select the discrete NVIDIA adapter? (p2, blocks nothing)
- **S-01b** — **answered, and the answer is worse than the question.** Merged `d7fe11d`,
  verdict **CONDITIONAL**. Branded Edge 152 loads the unpacked MV3 extension
  where branded Chrome 152 does not — but `context.route()` **does not cover the MV3
  offscreen document**, which `docs/architecture/constitution.md` §5 makes the send path.
  Under an abort-everything route handler the offscreen POST reached the wire in **3 runs
  of 3**. A suite written on `context.route()` would assert *zero outbound requests*, pass,
  and prove nothing. **QG-04 cannot be signed off on the current enforcement plan.**
  → **B-02**, issue #5. The literal S-01b question (Playwright's *bundled* Chromium) is
  still `UNKNOWN` — the binary would not execute on workstation 2.

**Invariant E itself is unchanged and has not been weakened.** Mechanisms (1) lint,
(3) manifest CSP and (4) payload hash pin are unaffected; mechanism (2) has a measured
coverage gap over the send path, and choosing its replacement is an ADR decision reserved
for the human architect.

- **B-02** — **answered.** Open **PR #9**, verdict **CONDITIONAL**. CDP attached to the
  offscreen target observes 5 of 5 and genuinely blocks (the collector confirms zero
  arrivals); an independent loopback collector detects an unauthorised sender by its absent
  provenance headers and caught a tampered payload by recomputing SHA-256 over the received
  bytes. **No single mechanism answers all five sub-questions; `CDP + collector` does**, with
  independent failure modes. **Not adopted — that needs an ADR (B-02-2).** A deterministic
  regression guard now encodes the false-green failure mode, 3 of 3 runs.

Operational risk, **workstation 1 only**: its working tree is on a OneDrive-synced path,
which can race with Git on `.git/` internals. Workstation 2 is on a plain local path and
is unaffected. See `docs/operations/git-workflow.md` §8.4.

---

## Decisions log pointer

`docs/adr/README.md` — **1 ADR recorded, approved and implemented.** 12 ADR candidates
identified, one written.

- **ADR-0001** — WebAssembly CSP directive and the `connect-src` provenance pin.
  **APPROVED 2026-09-10** by the human architect at the architectural decision level, subject
  to G1–G7. Implemented; all nine gate cells recorded in §12.1, with **G6 a CONDITIONAL
  PASS** (CSP diff only; the `privacy-security-engineer` veto is not waived). Approval was
  explicitly not approval to weaken any gate — **QG-04 stays UNSIGNED, B-02 stays OPEN,
  Firefox on Linux stays UNKNOWN.**
- **ADR-0002** — QG-03b-2c, T1 capture format policy (explicit PNG only). **PROPOSED
  2026-09-11**, awaiting human-architect approval.

---

## Session log

| Date | Session | Outcome |
|---|---|---|
| 2026-09-07 | Initialization / discovery pass | Dossier v4.0 read in full and recorded; Raptor's Way audited against its implementation, not its README; PratiBimb AgentOS layer created; contracts frozen; registries created empty. **No product implementation started.** |
| 2026-09-07 | Reconstruction onto workstation 2 | Repository reconstructed from GitHub on a second machine via a fork. S-01 evidence re-verified independently — every headline figure recomputed from the raw run records and reproduced exactly. Raptor's Way audit re-measured; one figure corrected forward (`AUDIT-0001` Correction C-1). Five documentation defects fixed (PR #3, merged `09aa71b`). Blocker register created. **No product implementation started.** |
| 2026-09-07 | S-01b executed | Invariant E's planned Playwright interception measured against the real send path. **CONDITIONAL** — `context.route()` is blind to the MV3 offscreen document, 3 runs of 3. Invariant unchanged, QG-04 unsigned, replacement vehicle deliberately left to an ADR. **No product implementation started.** |
| 2026-09-10 | ADR-0001 approved and implemented | The project's **first production code**. `packages/security` implements the approved CSP builder, the G1 WebAssembly capability assertion (`compile`, never `validate`) and the ORT WASM byte pin with its C-1 realm guard; `scripts/generate-ort-pin.mjs` breaks the build on bundle **or ORT API** drift; `tests/browser/gates` carries G1–G3 as permanent regression guards across Chromium and Firefox. All nine gate cells recorded, G6 **CONDITIONAL**. B-02 guard re-run and still passing 3/3. **QG-04 unsigned, B-02 open, Firefox-on-Linux UNKNOWN — none promoted.** Perception/product work deliberately not started. |
| 2026-09-10 | S-05 perception substrate | **The perception tier exists.** `packages/perception` implements the coordinate contract (four spaces, branded so a device-pixel value cannot be passed as CSS pixels), capture with its full geometry block, the derived element graph, the off-screen observation model, the capability-aware detector interface, DOM/vision fusion at the frozen IoU > 0.5 with provenance preserved rather than collapsed, the T0 change gate, and the perception-state assembly with a handoff type that structurally cannot carry pixels. **QG-02: criteria 1–6 PASS on Chromium across six DPR/zoom configurations, criterion 7 CONDITIONAL pending an executor.** 142 perception tests, 178 across the workspace. **UIElementDetector still has NO admissible implementation** — option A is AGPL-excluded, B is week 3, C is the DOM-only floor. Firefox QG-02 NOT RUN on this workstation. **QG-04 unsigned, B-02 open, no model adopted.** |
| 2026-09-10 | W1-S05-rate capture limits | **S-05's own question answered — `CONDITIONAL`.** `captureVisibleTab` characterized on Chromium 151 (MV3 service worker) and Firefox 155 (MV2 background) over bounded schedules, ladder repeated 3x per run, Chromium x2 runs. **Chromium: safe envelope 500 ms spacing (~1.7 captures/sec), onset between 2 and 3 Hz, ceiling ~2.6/s, burst allowance 2, recovery ~1.15 s, stateful.** **Firefox: no throttle observed at or below 10 Hz** — recorded as an absence of observation, not a proof of absence. The **first run was invalid** (0% success everywhere) and the A-F cause rule caught it: 212 of 304 failures were a permission error, not a rate limit. **`activeTab` NOT MEASURED**, which caps this at `CONDITIONAL`. Production: `CAPTURE_THROTTLED` added as a distinct refusal with **no retry/backoff/queueing** in the adapter; `minCaptureIntervalMs` 250 -> 500 ms, the only constant the evidence bounds. **QG-04 unsigned, B-02 open, no model touched.** |
| 2026-09-10 | T1 detector head + fusion | **The fusion half is complete and gated; the detector half is a contract with no admissible implementation.** Added the T1 `UIElementDetector` tensor contract (YOLO-family, 640 square letterboxed, anchor-free `[1, 4+C, A]`, matching option A's so it stays droppable-in), a fifth branded coordinate space for model-input pixels with the model->capture->CSS chain explicit, per-class deterministic NMS pinned to the frozen fusion IoU, and pinned model identity on every detection. **Fixed a real gap in shipped code: `fuse()` had never compared detection frame identity against the graph's, so an old screenshot could fuse silently with a current DOM** - now `STALE_FRAME`, verified on real browser data. **The detector has NO WEIGHTS and reports `MODEL_ASSET_UNAVAILABLE`**; option A stays licence-excluded, option B untrained, **option C (DOM-only) is the shipped configuration**. **Not benchmark-complete** - the prescribed metric (element mAP@0.5, element recall, grounding accuracy over ScreenSpot-v2 plus 300 self-labelled screens) is QG-05, which does not exist. T2's accessible-name hazard is now pinned open by test rather than fixed here. 234 perception tests, 270 across the workspace. **QG-02 unchanged (criterion 7 still CONDITIONAL), QG-03 unaffected, QG-04 unsigned, B-02 open, model registry untouched.** |
| 2026-09-10 | QG-05 T1 evaluation harness | **The evaluator exists before the detector, deliberately.** `packages/evaluation` implements the QG-05 **visual-context slice (25%)** with the metrics the dossier names — element mAP@0.5 (all-point interpolated), element recall, grounding accuracy — plus a deterministic synthetic generator (same version + seed ⇒ identical manifest hash), fail-closed label validation, train/dev/test splits with **content-fingerprint** leakage detection, and threshold sweeps confined to dev. Validated by controlled baselines on a real browser fixture: perfect 1.0/1.0/1.0, empty 0/0/NaN, 10 px shift → recall 0.70, 400 px shift → 0. **Those are EVALUATOR baselines, not detector performance.** **The gate is NOT passed** — one of six scored metrics is implemented; the rest need T2, the server or the executor. **Detector performance remains UNKNOWN**; `UIElementDetector` still refuses with `MODEL_ASSET_UNAVAILABLE` and the **model registry is untouched**. Also: T1/D3-D4 terminology corrected globally (3 genuine mislabels; all legitimate privacy references left alone), and the 2026-09-10 unauthorized force-push recorded in git-workflow §5.1. **QG-02 unchanged, QG-04 unsigned, B-02 open.** |
| 2026-09-10 | T1 detector option B trained | **The learning pipeline is proven; the detector is NOT adopted.** Rendered 200-sample synthetic set (hash `4bbc57de`), trained the project's own 61K-parameter anchor-free head (0.30 MB ONNX, sha256 `ba6d9e93...`), evaluated through the existing QG-05 evaluator on the held-out split. **The micro-overfit gate found two real bugs** — a box parameterisation that could not represent its own targets (76% of objects span >1 cell), and stride 16 being too coarse for ~10 px controls (checkbox and radio scored 0/15). Both fixed at root cause; final gate PASS on all six criteria with thresholds unchanged. **Held-out (SYNTHETIC): mAP@0.5 0.7955, recall 0.9229, grounding 0.0547.** The pre-registered threshold rule (max mAP on dev) proved defective — it selects the lowest threshold and destroys precision; **no threshold was re-picked after test was seen**, and the corrected rule is deferred to the next iteration. Dominant failure mode is `button` (AP 0.4993, a third of all false positives); small objects are no longer the problem after the stride fix. **Adoption bar NOT met** — no browser/backend matrix, no latency or memory, grounding unusable — so `UIElementDetector` still reports `MODEL_ASSET_UNAVAILABLE`, the DOM-only floor still ships, and **the model registry is unchanged**. Also repaired a broken governance guard (`verify-repo` false-positived on locally-trained weights). **QG-04 unsigned, B-02 open, T2 not started.** |
| 2026-09-11 | QG-03b-2c capture format policy (workstation 2) | **Decided and proposed, not approved.** ADR-0002 records T1 production capture as **explicit PNG only**. The dossier mandates no capture encoding; its WebP is the T2 egress frame. The adapter now **refuses** any non-PNG payload, with no re-request and no fallback; `CaptureFrame.format` is narrowed to `"png"` (closes QG-03b-2d); MIME/signature agreement is enforced. JPEG's faster capture (21.5 vs 37.0 ms, workstation 1) is recorded as the cost. Compression robustness leaves QG-03a's T1 scope on approval and remains a T2 question. 10 behaviour tests and a compile-time guard, each shown to fail under a targeted mutation. No new measurement, no network change, no security semantics touched. **QG-03b-2c CONDITIONAL pending approval; QG-03 CONDITIONAL; detector UNADOPTED; QG-04 unsigned; B-02 open.** |
| 2026-09-11 | QG-03a T1 production-path robustness (workstation 2) | **QG-03a `OPEN`: A PASS after one fix, B OPEN, C CONDITIONAL.** No model was run (the artifact is absent and was not regenerated) and the held-out split was not evaluated. **A:** a real defect, found and fixed. The shipped raster rounded exact-half extents up where training rounds them to even, diverging on real capture sizes (1280×641, 1024×644, 2560×1442, …) identically in Node, Chrome 153 and Firefox 155. `roundHalfEven` makes all three 40/40 bitwise. **B:** hard greedy NMS is discontinuous at near-ties (root cause localised). The swapped survivor stays on the element, but the real-model magnitude is unmeasured, and a noise-robustness criterion is raised for the architect. **C:** the 84/200 label/raster offset is bounded and metric-neutral in the worst case, so no retrain is justified on its own. **QG-03 CONDITIONAL; detector UNADOPTED; threshold 0.55; QG-04 unsigned; B-02 open.** |
| 2026-09-12 | QG-03a-B2 real model under real backend noise (workstation 2) | **B2 PASS here; QG-03a-B CONDITIONAL; QG-03a OPEN.** The exact artifact (sha256 `ba6d9e93…`, 302,960 B, placed by the owner and hash-verified, never committed) was run through the production ORT pin and session factory: Chrome 153 and Firefox 155, WASM and WebGPU (backend proven by GPU submits), plus native CPU. WASM is bitwise identical across browsers; WebGPU differs by ≤ 1.8e-5 on scores. Through the shipped decode: **0 survivor swaps, 0 true failures, pre-registered criterion 20/20 in every pair.** The 1,402 exact score ties are reproduced by every backend, and iid noise, which destroys them, is not a backend-noise model. Margin: the non-UI stress page fails at 2× the real delta, real UI at 50×. Open: the extension context, other GPUs, and the criterion (ARCHITECT APPROVAL REQUIRED). No NMS change; detector UNADOPTED; threshold 0.55. |
| 2026-09-12 | QG-03a-B3-1 MV3 extension harness (workstation 2, DEVELOPMENT) | **Harness only. QG-03a-B3-1 is NOT STARTED and no status was promoted.** B3-1 needs **workstation 1**; the run recorded here is workstation 2 and is labelled `DEVELOPMENT / NON-W1 EVIDENCE` by the harness itself. Built the missing B3-1 harness by composing B2's measurement with QG-03b-2a's real MV3 architecture: the exact artifact (`ba6d9e93…`, never committed) through the production ORT pin and session factory, in the **offscreen document** and in **its dedicated worker** (`ortRuntime.ts` names the worker; QG-03/QG-03b-2a used the document, so both are separate cells), against a **native ORT 1.29.0 CPU** reference, keeping the **raw outputs** — the three things QG-03b-2a's workstation-1 evidence cannot supply (ORT 1.20.1 reference, summaries only, no GPU identity). B2's `b2_native_reference.py` now **refuses any onnxruntime but 1.29.0**. 21 guard tests, and both fail-closed paths fired on a real failure (an unusable browser produced four REJECTED cells and an analysis that refused to compare them). Development measurement, Chrome for Testing 153 on AMD `rdna-3`: 180 GPU submits per WebGPU cell and 0 per WASM cell with the hook verified present, input tensors identical to the committed digests 20/20, and against native **0 survivor swaps, 0 true failures, worst displacement 0.0028 CSS px**; the two realms are **bitwise identical** here. **QG-03a-B1 still ARCHITECT APPROVAL REQUIRED; B4 proposed; QG-03a-B CONDITIONAL; QG-03a OPEN; QG-03 CONDITIONAL; detector UNADOPTED; threshold 0.55; QG-04 unsigned; B-02 open.** |
| 2026-09-12 | QG-03a-B3-1 executed (workstation 1) | **B3-1 `PASS`. QG-03a-B still CONDITIONAL; QG-03a OPEN; QG-03 CONDITIONAL; detector UNADOPTED.** **QG-03a-B1 and B4 are APPROVED by ronitsaha11**: the QG-03b-2 numbers are reused **unchanged** for backend-noise robustness, the **18 UI fixtures gate**, all 20 are executed and reported, and `gradients-edges` is **STRESS-ONLY** and does not decide the UI gate. Measured on `LAPTOP-6E14K34L` (Intel Core 7 240H) in **Chrome for Testing 151.0.7922.34**, ORT Web **1.29.0** on the production pin, the exact artifact `ba6d9e93…` re-verified in every realm, against a **native onnxruntime 1.29.0 CPU** reference. Four independent cells (offscreen document and its dedicated worker × WASM and WebGPU): **180 GPU submits per WebGPU cell and 0 per WASM cell**, adapter **`intel / gen-12lp`**; input tensors identical to the committed digests 20/20; 3 byte-identical repetitions per fixture. Against the reference every cell gives **100% matched, count change 0, 0 survivor swaps, 0 true failures, worst displacement 0.002893 CSS px**; the **18-fixture gate passes 18/18 in all four cells** and the complete 20-fixture corpus passes 20/20 analytically. The two realms are **bitwise identical**. **`QG-03a-B3-2` (NVIDIA) is NOT MEASURED / OPEN** — Chrome chose Intel, so the RTX 5050 was never exercised; no NVIDIA cell was simulated. The harness was **not** modified (no evidence-integrity defect found); the fail-closed guards were re-fired against real W1 evidence. Raw `.f32` dumps gitignored, not deleted, archived outside Git. No NMS, threshold, model or registry change; threshold 0.55; QG-04 unsigned; B-02 open. |
| 2026-09-12 | QG-03a-B3-2 NVIDIA coverage decision (governance only) | **Owner decision recorded; no experiment was run and no hardware was touched.** The governing feasibility axis is **model × browser × backend**, and the dossier defines **no GPU-vendor axis**, so an NVIDIA-backed browser cell is **not a mandatory acceptance cell** for QG-03a-B: **`QG-03a-B3-2` = NOT MEASURED / OPTIONAL COVERAGE**, and **B3-1's Intel `gen-12lp` WebGPU cell stands as the required WebGPU evidence**. The RTX 5050 remains **unmeasured**; its absence affects **coverage and labelling only**, does **not** invalidate **B3-1 (`PASS`)**, and **no NVIDIA compatibility or performance claim may be made**. No retroactive B3-2 gate was created, no numerical threshold changed, no criterion was altered, and the harness was not touched. GPU-vendor selection stays an open coverage item under **S-01a**. Records corrected: the `QG-03a-B3-2` matrix row, the B3-1 `decision.md` follow-ups, and stale workstation-2-era statements in the B3-1 `README.md` (one of which still read *No B3-1 cell has been measured* under a `PASS` header). **QG-03a-B stays CONDITIONAL — promoting it is a separate architect decision; QG-03a OPEN; QG-03 CONDITIONAL; detector UNADOPTED; threshold 0.55; QG-04 unsigned; B-02 open.** |
| 2026-09-12 | QG-03a-C3 label/raster generalisation (workstation 2) | **C3 = INCONCLUSIVE / DECISION REQUIRED. QG-03a-C stays CONDITIONAL; nothing was retrained, and retraining is NOT justified by this result.** Pre-registered before the first render: 6 cells x 20 samples paired by generator seed, the exact artifact `ba6d9e93…` through the shipped path (`rasterLetterbox`, `decodeHeadOutput`, NMS, `projectToCapture`) under **ORT Web 1.29.0 WASM in Node**, scored by the **frozen** evaluator with the CLIPPED definition untouched and its per-cell ceiling (0.926-0.962) reported. **New positive finding:** at 1264x800 — off the training grid but at training-like scale, and the real production capture size of the QG-03b-2a fixtures — the artifact is **no worse than the on-grid controls** (mAP@0.5 0.6647 vs 0.5993/0.6610; grounding 0.6396 vs 0.4933/0.5645), so the learned nuisance mapping is **not grid-specific**. **Why INCONCLUSIVE:** capture size moves the label/raster offset (at most 1.33 CSS px) and the object scale in model space (1.50 to 4.00 CSS px per model px) **together**, and the collapse at 3-4 CSS px per model px tracks **scale**, not the convention — decisively in the a2/a3 pair, which share 3.00 CSS px per model px and score near-identically (mAP 0.0608 vs 0.0617) despite different capture sizes, DPR and label/raster offsets. Two harness statistics (minimum matched IoU, worst displacement) proved **uninformative** because the matcher gates at IoU 0.5, so the criterion clause about IoU for the smallest controls could not be evaluated and **no substitute was invented**. Follow-ups: **C3b** (re-invert the retained raw outputs with the raster transform — isolates the convention, no new inference) and **C3c** (scale-controlled cells with a pre-registered IoU statistic). Separately recorded, not C business: the detector is **scale-fragile** beyond ~2 CSS px per model px, which belongs to adoption items 11/14. Frozen evidence untouched: the consumed held-out split, the 200-sample dataset, the registry, threshold 0.55 and the evaluator. **QG-03a OPEN; QG-03 CONDITIONAL; detector UNADOPTED;** **QG-04 unsigned; B-02 open.** |
| 2026-09-12 | QG-03a-C3b letterbox-inverse attribution (workstation 2) | **C3b = NOT ATTRIBUTED TO LETTERBOX CONVENTION. No inference was run; no weights were loaded.** C3 retained raw outputs were re-inverted two ways on identical inputs: the production **continuous** inverse (`computeLetterbox`, uniform scale, real pads) against a **raster-consistent** inverse (`rasterLetterbox` integers, per-axis scales) — the change `preprocessing-contract` §6 prescribes. Everything else held byte-identical: same digest-verified `.f32` outputs, one shared `decodeHeadOutput` and NMS, same detection set, same 0.25 floor and 0.55 point, same sealed datasets, same DOM ground truth, same frozen evaluator. **Decisive observation:** the two most collapsed cells have an inversion delta of **exactly zero** — at 1920x1080 and 2560x1600 the capture scales to integer content (640x360, 640x400), so the two letterboxes **are the same transform** — yet they score mAP@0.5 **0.0617** and **0.0040** against controls at 0.599-0.661. Where the conventions differ most (2880x1443 at DPR 1.5, delta **1.998 CSS px**, the bound across all 120 samples), switching the inverse moves mAP by **+0.0011**, about 0.2% of that cell gap. No gap closed in either view, and the scale pattern is untouched. Incidental and explicitly **not** a recommendation: the raster inverse is marginally better where the two differ on real UI (a1 +0.0117 mAP) and marginally worse elsewhere (b1 -0.0003) — far too small to justify touching a frozen coordinate transform. **Consequence:** the §6 mismatch stays **recorded technical debt, not a production defect**, and **retraining is not justified** by C3 or C3b. What remains open is not C business: the detector is **scale-fragile beyond ~2 CSS px per model px** (adoption items 11/14, capture policy). Closing **QG-03a-C** is now an owner judgement call rather than a measurement gap - close it as CONDITIONAL-resolved with §6 as standing debt, or hold it until a training revision happens anyway. **QG-03a-C3 stays INCONCLUSIVE; QG-03a-C CONDITIONAL; QG-03a OPEN; QG-03 CONDITIONAL; detector UNADOPTED; threshold 0.55; QG-04 unsigned; B-02 open.** |
| 2026-09-12 | QG-03a-C geometry finding recorded (governance only, workstation 2) | **The label/raster GEOMETRY CONCERN is recommended as RESOLVED — NO DEMONSTRATED PRODUCTION DEFECT. `QG-03a-C` itself stays `CONDITIONAL`, because changing a gate status is the owner's decision and it was NOT self-approved.** No experiment was run, no model, threshold, evaluator, dataset or gate artifact was touched, and nothing was retrained. Documentation only. **What the recommendation rests on:** the mismatch is reproduced and bounded (84/200, <= 0.681 model px), metric-neutral at worst case (mAP@0.5, recall, grounding 1.000 on train and dev), correct end-to-end at production capture sizes (B2 0.0026 CSS px, B3-1 0.002893), safe off the training grid at production-like scale (C3's 1264x800 beats both controls), and now **directly exonerated as a cause** by C3b — inversion delta bounded at 1.998 CSS px and **exactly zero** in the two most collapsed cells, where the two letterboxes are provably the same transform. **It is NOT fixed and must not be called fixed:** the §6 defect stays in `tools/detector/data.py` as **technical debt / future training-revision item**, with the prescribed change unchanged, to bundle with the next training change (QG-03a-C1). **Why approval is required rather than optional:** no pre-registered criterion converts C from CONDITIONAL to PASS, C's CONDITIONAL is pinned to that outstanding debt, and the one remaining unknown — the effect on training **dynamics** — can only be measured by a retrain that is not justified on its own. Two options are on the record (close C as CONDITIONAL-resolved with §6 as standing debt, or hold C until a training revision happens anyway). **Separately recorded as a NEW open item, not as a C result:** `QG-03a-C4`, detector **scale fragility beyond ~2 CSS px per model px**, which belongs to **adoption item 11** (acceptable detector metrics, currently FAIL) and to capture policy. Audited and reported rather than assumed: **no capture-size or object-scale policy exists today** — ADR-0002 is a format policy (PNG only) and the dossier's only capture constraint is a latency budget — so **no production capture limit was invented**; creating one is a future adoption decision. **QG-03a-C3 stays INCONCLUSIVE; QG-03a-C CONDITIONAL; QG-03a OPEN; QG-03 CONDITIONAL; detector UNADOPTED; threshold 0.55; QG-04 unsigned; B-02 open.** |
| 2026-09-12 | QG-03a-C RESOLVED by owner + detector adoption-bar audit (governance only, workstation 2) | **OWNER DECISION RECORDED (ronitsaha11, PR #49 merged as `e22c3a4`): `QG-03a-C` = `RESOLVED / CONDITIONAL-RESOLVED` — the label/raster geometry concern is closed as NO DEMONSTRATED PRODUCTION DEFECT. The mismatch is NOT fixed and is retained as TECHNICAL DEBT / FUTURE TRAINING-REVISION ITEM.** Grounds, as recorded: the mismatch is real; C3 was inconclusive only because capture scale and label geometry were **confounded**; C3b changed **only** the inverse letterbox convention on **identical raw outputs** and moved the result by at most **1.998 CSS px** and **+0.0117 mAP@0.5** against a C3 degradation of **0.54-0.60 mAP**; and the cells where the two transforms are **exactly identical** degrade just as much. No numeric acceptance threshold was created for C, no retrain is justified by this issue alone, and none was performed. Historical C3/C3b verdicts are **unchanged** — both gained only a dated follow-up note. The separate **`QG-03a-C4`** scale-generalisation concern stays **OPEN** and is **not** folded into C. **Also recorded: `artifacts/reviews/AUDIT-0002-detector-adoption.md`** — a read-only audit of adoption items 9/11/12/13/14. Findings: **item 12 is satisfied** (real-artifact correctness in Chromium 151 and Firefox 155, WASM and WebGPU, headful and headless, plus B2 and B3-1 inside the real MV3 extension) and its "only Python onnxruntime CPU" note is **stale**; **item 9 is stale too** but is `CONDITIONAL` not PASS, because `model-adoption.md` blocking rule 1 requires all four columns and **Firefox WASM on LINUX has never been run** (Windows Firefox WASM is a different cell), with Firefox WebGPU headless `REJECT` on Windows; **item 13 is PARTIAL** - p50 and WASM heap measured on workstation 1, but `release()` returned no memory in any cell and **S-04 / QG-03e teardown-and-coexistence is UNKNOWN**, which `model-adoption.md` step 5 requires; **item 11 stays FAIL** and is the gating item — grounding 0.055 is a **threshold artefact** of the superseded rule (the frozen replacement is F1(recall, grounding), which selects 0.55 on dev at grounding 0.5678) but it **cannot be closed by re-scoring**, because no **unread** split exists, the required evidence source (ScreenSpot-v2 web subset + 300 self-labelled Indian government and banking screens, clean **and** redacted) **does not exist**, and the frozen CLIPPED definition caps dev recall at **94.4%**; **item 14 is `CONDITIONAL_PASS`** and follows items 9 and 11 rather than moving on its own. **Explicitly recorded: synthetic C3/C4 results CANNOT substitute for real evaluation evidence, and nothing on the bar requires a retrain today.** Minimum next workstream specified, not started: **W-A** real-UI evaluation data with a genuinely unread split (the only route to item 11; leaves QG-05 partial because the redacted half needs T2), and **W-B** a scale-controlled evaluation for C4 with a properly pre-registered matched-pair IoU statistic (C3's `minMatchedIou` / `worstMatchedDispCss` were structurally uninformative). **No capture-size or scale limit was invented — none exists today (ADR-0002 is a format policy).** Nothing was implemented, retrained, re-scored or re-run; no gate item status was moved, because those live in a frozen gate record and moving one is an owner decision. **QG-03a-C RESOLVED/CONDITIONAL-RESOLVED; QG-03a-C3 INCONCLUSIVE; QG-03a-C3b NOT ATTRIBUTED; QG-03a-C4 OPEN; QG-03a OPEN; QG-03 CONDITIONAL; detector UNADOPTED; threshold 0.55 with the shipped constant still 0.25; QG-04 unsigned; B-02 open; QG-05 PARTIAL.** |
| 2026-09-12 | W-A designed: real-UI evaluation data for adoption item 11 (design only, workstation 2) | **DESIGN / PRE-REGISTRATION ONLY. No screen was collected, no annotation made, no split sealed, nothing trained, nothing evaluated.** `artifacts/experiments/WA-item11-real-ui-evaluation/` fixes the rules for a legitimate item-11 replacement figure BEFORE the data exists: dataset definition, annotation protocol, split and leakage discipline, threshold discipline, metric set, CLIPPED handling, privacy/redaction pipeline, licensing, PASS/FAIL/INCONCLUSIVE structure and the approval sequence. **Three findings shape it.** (1) **The evaluator needs NO change** — `SampleProvenance` already admits `REAL_LABELLED {source, annotator}` and `SCREENSPOT_V2 {subset, originalId}`, and `Sample.framePath` is optional, so item 11 is **data plus governance** with zero modification to a frozen evaluator. (2) **No numeric acceptance criterion for item 11 exists anywhere** — the bar says only *acceptable detector metrics*, and the benchmark contract sets a target for PII recall (>= 0.98) and none for visual context; defining it is an **owner decision that must precede sealing**, or the figure arrives and then gets argued about. (3) **The existing `assertNoLeakage` cannot protect real data** — its content fingerprint is geometry plus annotations, so the SAME real page captured at a different scroll offset, viewport or zoom would pass; real data needs **page- and origin-level** split discipline as a dataset-construction guard, not an evaluator change. **Also recorded:** ScreenSpot-style sets are instruction-grounding (one target box per instruction), so **element recall and mAP@0.5 are not computable on them as published** — flagged as an INFERENCE to verify at the pinned revision, with three owner options (re-label exhaustively / report a separate point-grounding figure / scope out). Its **licence is UNKNOWN in this repository** and must be read at a pinned revision by the project's own documented method; **no legal claim was made**. The **redacted half of item 11 is blocked on T2** regardless of data. **Privacy hard stop:** no real screen may be captured until the privacy and licensing approvals are recorded; frames are never committed (precedent: C3 raw outputs archived outside Git), redaction happens at ingest and is re-verified by the frozen verifier sequence, and real personal data is excluded at source rather than redacted after the fact. The frozen threshold rule **F1(element recall, grounding accuracy) applies UNCHANGED** to a real dev split — no owner decision is needed for that; **0.55 stays frozen** unless the rule selects otherwise on real dev data, and `PROVISIONAL_THRESHOLDS.score` stays **0.25**. **CLIPPED stays frozen** and its ceiling is reported, not fixed. **10 owner decisions (D1-D10) are listed and none was self-approved.** Item 11 stays **FAIL**; detector **UNADOPTED**; artifact `ba6d9e93…`, threshold 0.55, evaluator, CLIPPED definition, consumed held-out split, `artifacts/datasets/` and `artifacts/gates/` all untouched. **QG-03a-C RESOLVED/CONDITIONAL-RESOLVED; QG-03a-C4 OPEN; QG-03a OPEN; QG-03 CONDITIONAL; QG-04 unsigned; B-02 open; QG-05 PARTIAL.** |
| 2026-09-12 | D5 + D2 owner-decision package prepared (governance only, workstation 2) | **The real-world collection gate remains CLOSED. No data was collected, no site was visited, no page was captured, no PII, no credential, no TEST read, no new split.** PR #56 is merged, which makes the D2/D5 **framework** governing — and **nothing else**: it approved no source, adopted no gate structure, set no numeric threshold and authorised no collection. Added `artifacts/governance/D5-D2-OWNER-DECISION-PACKAGE.md`, the **signable** form the package was missing: one decision page per retained source (G1 `mygov.in`, G2 `india.gov.in`, B1 `sbi.bank.in` public product pages only) carrying the proposed surface, the retrieved policy with **verbatim** quotation and URL, robots evidence, the named permission route, proposed conditions, the **unresolved legal question left unresolved**, and a tick-box decision block with a condition field. Engineering recommendation is **DEFER on all three** — G1 and B1 because written permission/approval is required and has not been requested, G2 because its policy **was never retrieved** (HTTP 403) and a search summary is not authoritative policy. **No source is marked approved, and no legal conclusion is drawn.** The eight W-A baseline conditions are carried forward unchanged as non-negotiable. For **D2**, added the owner sheet explaining what `G`, `R`, `M`, `S` and `G_min` each control, what calibrates them, what must be frozen first, why TEST opens **once**, and why a bar tuned after TEST stops being a test — **all five values left PENDING and unchosen**, with Option C still only RECOMMENDED. Recorded that the cross-machine observation (grounding −0.0015, recall −0.0013, ±2 localization FPs, 40 synthetic screens) is **one paired synthetic observation and cannot define `S`**; repeated DEV evidence under the real protocol is required, and no repeats were manufactured to produce an uncertainty number. **One inconsistency inside the merged package was found and corrected:** the registry's owner decision form still listed all **eight** pass-1 candidates while pass 2 had reduced the set to three, so signing a dropped row would have authorised a source the evidence does not support — the stale table is now marked **SUPERSEDED** and points at the signable form; the pass-1 record is preserved unchanged. The gate document gained a dated note that PR #56 did not open it. **Four states kept explicitly distinct: engineering-ready (mostly yes), owner-approved (NO), legally approved (NO), collection-authorised (NO — gate CLOSED).** Detector **UNADOPTED**; V1 **experimental, not shipped**; production NMS, threshold, model, preprocessing all unchanged; **no retraining**; Track B complete (W1, H-GEOMETRY SUPPORTED, appearance a substantial secondary effect); **no capture-size policy exists**; detector packaging still unimplemented with the runtime hash-verification gap open and ADR-0003 still PROPOSED. **QG-03a OPEN; QG-03 CONDITIONAL; item 11 FAIL; QG-04 unsigned; B-02 open; QG-05 PARTIAL.** |
| 2026-09-12 | MVP-0: the agent loop is NOT demonstrable, and DOM insufficiency measured (workstation 2) | **HEADLINE: the end-to-end MVP cannot be demonstrated, because 9 of the loop's 11 stages have NO IMPLEMENTATION — and nothing was stubbed to hide that.** `AUDIT-0005` records it stage by stage with file evidence: **OBSERVE** partial (the capture adapter exists but **there is no `manifest.json` anywhere**, so no extension hosts it), **PERCEIVE** yes, then **SANITIZE / VERIFY / REASON / PLAN / VALIDATE / REFRESH / RE-HYDRATE / ACT / VERIFY-RESULT all absent** — no vault (`grep -ril vault packages/*/src` = 0), no PII detector, no redaction, no verifier, no egress module, no action validator, no executor, no server (B-03). All 25 security invariants remain **SPEC**, not implemented. **A stubbed privacy firewall was deliberately NOT written:** in a privacy submission a hand-rolled vault and egress path would be indistinguishable to a judge from the real thing and would not be the real thing. **MVP-0** therefore measures only the two stages that exist, on pages served from **127.0.0.1** by the harness (Mode A = the repo's own QG-02 fixture; Mode B = a canvas-painted registration form generated locally from fabricated values). **Result, and the task FAILED:** Mode B's DOM exposes **0** actionable elements, grounds **0/7** targets and types **0/6** fields, so DOM insufficiency is now **measured rather than assumed**; the real detector (experimental; artifact, threshold, NMS and decode all unchanged) grounds **4/7** at the frozen IoU 0.5 fusion rule (fullName 0.857, email 0.787, phone 0.686, dob 0.633) and types **4/6** — but **refused to click submit** because nothing overlapped it (best IoU **0.0665**), with `address` 0.485 and `pin` 0.425 **near misses left as failures**. **The gate was not lowered to manufacture a success.** 46 candidates for 7 real targets, consistent with W-1's precision finding. Detector latency 25 ms preprocess / 44 ms inference / 4 ms decode. Fail-closed observed: 10 refusals (`NO_CANDIDATE_AT_IOU_0.5`), never a fallback to the nearest box. **No privacy claim is made — there is no firewall to test.** Recommended build order, smallest first: **VALIDATE+REFRESH** (pure functions, no network, no model, no secrets, and the stage whose absence makes every other stage untestable), then ACT, then the memory-only vault, then D1+D2 sanitize (which need no model), then VERIFY, then REASON last. **No real data, no real PII, no credential, no external site, no security control touched.** Detector **UNADOPTED**; V1 **experimental**; no NMS, threshold or model change; **no retraining**; no capture-size policy; W-A gate **CLOSED**; D5/D2 unchanged. |
